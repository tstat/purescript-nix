# Working nix derivations for purescript and spago

Build purescript and spago by cloning this repo and running

```bash
nix-build
```

or to get into a shell that has purescript and spago:

```bash
nix-shell
```
